# Token do seu bot (copie no Discord Developer Portal → Bot → Copy Token)
BOT_TOKEN=SEU_TOKEN_AQUI

# Application ID do seu bot (Discord Developer Portal → General Information → Application ID)
CLIENT_ID=SEU_CLIENT_ID_AQUI

# ID do servidor onde o bot vai ficar (Discord → clique no nome do servidor → Copiar ID, com Modo Desenvolvedor ativo)
GUILD_ID=SEU_GUILD_ID_AQUI

# Sua chave PIX (CPF, e-mail, telefone ou chave aleatória)
PIX_CHAVE=SEU_PIX_AQUI
